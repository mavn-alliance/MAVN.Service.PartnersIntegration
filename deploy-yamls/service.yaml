apiVersion: v1
kind: Service
metadata:
  labels:
    app: partners-integration
    environment: Development
  name: partners-integration
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: partners-integration
  sessionAffinity: None
  type: ClusterIP
